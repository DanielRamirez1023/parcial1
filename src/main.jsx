import React from "react";
import ReactDOM from "react-dom/client";
import AppDR from "./AppDR.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppDR />
  </React.StrictMode>
);
